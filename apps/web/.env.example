# Calley Web — Development Environment Variables
# Copy this file to .env and fill in values as needed.

# ─── API Connection ──────────────────────────────────
VITE_API_URL=http://localhost:4000/api/v1

# ─── Web Push (VAPID) ────────────────────────────────
# Must match VAPID_PUBLIC_KEY in the API .env
VITE_VAPID_PUBLIC_KEY=
