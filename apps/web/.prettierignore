# Prettier resolves `.prettierignore` relative to its working directory, and the
# workspace `format` script runs from `apps/web`. The root ignore file covers
# root-level runs (lint-staged); this one covers `pnpm format` via Turbo.
#
# Generated by @tanstack/router-plugin on every build — its own formatter owns
# this file. Formatting it here only makes the next build dirty the tree again.
src/routeTree.gen.ts
